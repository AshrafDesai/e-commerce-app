[phases.setup]
nixPkgs = ["nodejs-18_x"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'No build step needed'"]

[start]
cmd = "node server.js"
```

---

## Also Add `backend/Procfile`

Create `backend/Procfile` (no extension):
```
web: node server.js